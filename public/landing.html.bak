<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Parlay Killer Live Edge — Price the next few minutes of the game</title>
  <meta name="description" content="Real-time live betting edge tool. See what the total should be before the market does.">
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;1,300&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --bg: #080808; --s1: #0d0d0d; --s2: #111111;
      --border: #141414; --b2: #1e1e1e;
      --green: #00e676; --red: #ff453a; --amber: #f5a623;
      --white: #ffffff; --dim: #555555; --muted: #2a2a2a;
    }
    html { scroll-behavior: smooth; }
    body { background: var(--bg); color: var(--white); font-family: 'DM Sans', sans-serif; font-size: 16px; line-height: 1.6; min-height: 100vh; }
    .wrap { max-width: 1080px; margin: 0 auto; padding: 0 32px; }
    a { color: inherit; text-decoration: none; }
    button { cursor: pointer; font-family: 'DM Sans', sans-serif; }

    /* LOGO */
    .logo { display: flex; align-items: center; line-height: 1; }
    .logo-pk { font-family: 'DM Sans', sans-serif; font-size: 13px; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; }
    .logo-pk .w { color: #ffffff; }
    .logo-pk .g { color: #00e676; }
    .logo-le { font-family: 'Bebas Neue', sans-serif; font-size: 16px; color: #ff453a; letter-spacing: 0.1em; margin-left: 7px; position: relative; top: 1px; }

    /* NAV */
    .nav { position: sticky; top: 0; z-index: 100; background: var(--bg); border-bottom: 1px solid var(--border); }
    .nav-inner { display: flex; justify-content: space-between; align-items: center; height: 56px; }
    .nav-actions { display: flex; gap: 10px; align-items: center; }
    .btn-ghost { font-size: 11px; font-weight: 600; color: var(--dim); letter-spacing: 0.08em; text-transform: uppercase; background: none; border: 1px solid var(--b2); padding: 8px 16px; transition: color 0.15s, border-color 0.15s; }
    .btn-ghost:hover { color: var(--white); border-color: #444; }
    .btn-green { font-family: 'Bebas Neue', sans-serif; font-size: 14px; color: #080808; background: var(--green); border: none; padding: 10px 20px; letter-spacing: 0.1em; transition: opacity 0.15s; display: flex; align-items: center; gap: 6px; }
    .btn-green:hover { opacity: 0.88; }
    .btn-green svg { flex-shrink: 0; }
    .nav-signin { display: block; }

    /* SECTIONS */
    section { padding: 80px 0; border-bottom: 1px solid var(--border); }
    .tag { display: inline-flex; align-items: center; gap: 6px; font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--dim); margin-bottom: 20px; }
    .tag-dot { width: 6px; height: 6px; background: var(--red); border-radius: 50%; animation: pulse 1.4s ease-in-out infinite; }
    @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.4;transform:scale(0.8)} }
    .section-head { font-family: 'Bebas Neue', sans-serif; font-size: clamp(32px, 5vw, 52px); letter-spacing: 0.04em; line-height: 1.05; margin-bottom: 16px; }
    .section-sub { font-size: 15px; font-weight: 300; color: #aaa; max-width: 560px; line-height: 1.7; margin-bottom: 40px; }

    /* HERO */
    .hero { padding: 72px 0 80px; border-bottom: 1px solid var(--border); }
    .hero-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; }
    .eyebrow-pill { display: inline-flex; align-items: center; gap: 8px; border-left: 2px solid var(--red); padding: 5px 12px; background: rgba(255,69,58,0.07); font-size: 10px; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: var(--white); margin-bottom: 24px; }
    .eyebrow-dot { width: 6px; height: 6px; background: var(--red); border-radius: 50%; animation: pulse 1.4s ease-in-out infinite; flex-shrink: 0; }
    .hero-headline { font-family: 'Bebas Neue', sans-serif; font-size: clamp(38px, 5.5vw, 64px); letter-spacing: 0.03em; line-height: 1.0; margin-bottom: 20px; }
    .hero-headline .hl-green { color: var(--green); }
    .hero-sub { font-size: 16px; font-weight: 300; color: #aaa; line-height: 1.7; margin-bottom: 32px; max-width: 460px; }
    .hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; align-items: center; margin-bottom: 16px; }
    .cta-primary { font-family: 'Bebas Neue', sans-serif; font-size: 15px; letter-spacing: 0.1em; color: #080808; background: var(--green); border: none; padding: 13px 24px; display: inline-flex; align-items: center; gap: 8px; transition: opacity 0.15s; }
    .cta-primary:hover { opacity: 0.88; }
    .cta-secondary { font-family: 'DM Sans', sans-serif; font-size: 13px; font-weight: 500; color: var(--white); background: none; border: 1px solid var(--b2); padding: 12px 20px; display: inline-flex; align-items: center; gap: 8px; transition: border-color 0.15s; }
    .cta-secondary:hover { border-color: #555; }
    .hero-note { display: flex; align-items: center; gap: 6px; font-size: 11px; color: var(--dim); }

    /* MOCKUP CARD */
    .mockup-card { background: var(--s1); border: 1px solid var(--b2); border-left: 3px solid var(--green); padding: 20px; }
    .mc-header { font-size: 10px; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--dim); margin-bottom: 14px; }
    .mc-score-row { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 8px; margin-bottom: 16px; padding-bottom: 16px; border-bottom: 1px solid var(--border); }
    .mc-team { font-family: 'Bebas Neue', sans-serif; }
    .mc-team-abbr { font-size: 28px; letter-spacing: 0.04em; color: var(--white); }
    .mc-team-score { font-size: 42px; letter-spacing: -0.01em; color: var(--white); }
    .mc-team.right { text-align: right; }
    .mc-center { text-align: center; }
    .mc-period { font-size: 10px; font-weight: 600; color: var(--dim); letter-spacing: 0.1em; }
    .mc-clock { font-family: 'Bebas Neue', sans-serif; font-size: 18px; color: var(--white); }
    .mc-data-row { display: flex; justify-content: space-between; align-items: center; padding: 6px 0; font-size: 12px; }
    .mc-data-label { color: var(--dim); font-weight: 400; }
    .mc-data-val { font-weight: 600; color: var(--white); }
    .mc-data-val.green { color: var(--green); }
    .mc-edge-block { background: var(--muted); border: 1px solid var(--b2); padding: 14px; margin: 14px 0; text-align: center; }
    .mc-edge-num { font-family: 'Bebas Neue', sans-serif; font-size: 32px; letter-spacing: 0.04em; color: var(--green); }
    .mc-edge-sub { font-size: 11px; color: var(--dim); margin-top: 2px; }
    .mc-window-row { display: flex; justify-content: space-between; align-items: center; font-size: 12px; margin-bottom: 12px; }
    .mc-window-label { color: var(--dim); }
    .mc-window-val { color: var(--green); font-weight: 600; }
    .mc-badge { display: inline-flex; align-items: center; gap: 6px; border: 1px solid var(--green); padding: 4px 10px; font-family: 'Bebas Neue', sans-serif; font-size: 12px; letter-spacing: 0.1em; color: var(--green); }
    .mc-badge-dot { width: 6px; height: 6px; background: var(--green); border-radius: 50%; animation: pulse 1.4s ease-in-out infinite; }

    /* THE PROBLEM */
    .problem-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: start; }
    .problem-prose { font-size: 15px; font-weight: 300; color: #aaa; line-height: 1.85; font-style: italic; }
    .line-table { border: 1px solid var(--b2); }
    .lt-row { display: flex; justify-content: space-between; align-items: center; padding: 12px 16px; border-bottom: 1px solid var(--border); font-size: 13px; }
    .lt-row:last-child { border-bottom: none; }
    .lt-label { color: var(--dim); font-size: 11px; }
    .lt-val { font-weight: 600; font-size: 14px; }
    .lt-val.white { color: var(--white); }
    .lt-val.amber { color: var(--amber); }
    .lt-val.red { color: var(--red); }
    .lt-val.muted { color: var(--dim); font-style: italic; font-weight: 400; font-size: 12px; }

    /* THE SHIFT */
    .steps-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; border: 1px solid var(--b2); }
    .step { padding: 28px 24px; border-right: 1px solid var(--b2); }
    .step:last-child { border-right: none; }
    .step-num { font-family: 'Bebas Neue', sans-serif; font-size: 11px; letter-spacing: 0.16em; color: var(--dim); margin-bottom: 12px; }
    .step-icon { margin-bottom: 12px; opacity: 0.7; }
    .step-title { font-family: 'Bebas Neue', sans-serif; font-size: 22px; letter-spacing: 0.06em; color: var(--white); margin-bottom: 8px; }
    .step-desc { font-size: 13px; font-weight: 300; color: #aaa; line-height: 1.6; margin-bottom: 14px; }
    .code-block { background: var(--muted); border: 1px solid var(--b2); padding: 12px 14px; font-size: 12px; font-family: 'DM Mono', monospace; line-height: 1.8; }
    .cb-green { color: var(--green); }

    /* FEATURES */
    .features-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1px; background: var(--b2); border: 1px solid var(--b2); }
    .feat-card { background: var(--s1); padding: 28px 24px; }
    .feat-card.accent { border-top: 2px solid var(--green); }
    .feat-icon { margin-bottom: 14px; opacity: 0.8; }
    .feat-title { font-family: 'Bebas Neue', sans-serif; font-size: 20px; letter-spacing: 0.06em; color: var(--white); margin-bottom: 6px; }
    .feat-desc { font-size: 13px; font-weight: 300; color: #888; line-height: 1.6; margin-bottom: 14px; }
    .feat-list { list-style: none; }
    .feat-list li { font-size: 12px; color: var(--dim); padding: 4px 0; display: flex; align-items: center; gap: 6px; }
    .feat-list li::before { content: '—'; color: var(--muted); flex-shrink: 0; }

    /* PROOF */
    .proof-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
    .proof-card { background: var(--s1); border: 1px solid var(--b2); padding: 24px; }
    .proof-title { font-family: 'Bebas Neue', sans-serif; font-size: 14px; letter-spacing: 0.1em; color: var(--dim); margin-bottom: 16px; text-transform: uppercase; }
    .proof-row { display: flex; justify-content: space-between; align-items: center; padding: 8px 0; border-bottom: 1px solid var(--border); font-size: 13px; }
    .proof-row:last-of-type { border-bottom: none; }
    .proof-label { color: var(--dim); }
    .proof-val { font-weight: 600; }
    .proof-val.green { color: var(--green); }
    .proof-quote { margin-top: 14px; font-size: 12px; font-style: italic; color: var(--dim); line-height: 1.6; border-left: 2px solid var(--b2); padding-left: 10px; }

    /* WHO */
    .who-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1px; background: var(--b2); border: 1px solid var(--b2); }
    .who-card { background: var(--s1); padding: 28px 24px; }
    .who-icon { margin-bottom: 14px; opacity: 0.7; }
    .who-title { font-family: 'Bebas Neue', sans-serif; font-size: 18px; letter-spacing: 0.06em; color: var(--white); margin-bottom: 8px; }
    .who-desc { font-size: 13px; font-weight: 300; color: #888; line-height: 1.6; }

    /* PRICING */
    .pricing-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; max-width: 520px; }
    .plan { background: var(--s1); border: 1px solid var(--b2); padding: 28px 24px; }
    .plan.featured { border-top: 2px solid var(--green); background: var(--s2); }
    .plan-name { font-family: 'Bebas Neue', sans-serif; font-size: 20px; letter-spacing: 0.1em; color: var(--white); margin-bottom: 4px; }
    .plan-price { font-family: 'Bebas Neue', sans-serif; font-size: 36px; letter-spacing: 0.02em; color: var(--white); margin-bottom: 4px; }
    .plan-note { font-size: 11px; color: var(--dim); margin-bottom: 20px; }
    .plan-list { list-style: none; margin-bottom: 24px; }
    .plan-list li { font-size: 13px; color: #aaa; padding: 5px 0; display: flex; align-items: center; gap: 8px; border-bottom: 1px solid var(--border); }
    .plan-list li:last-child { border-bottom: none; }
    .plan-list li::before { content: '→'; color: var(--dim); font-size: 10px; flex-shrink: 0; }
    .plan-btn { width: 100%; padding: 12px; font-family: 'Bebas Neue', sans-serif; font-size: 14px; letter-spacing: 0.1em; border: none; }
    .plan-btn.ghost { background: none; border: 1px solid var(--b2); color: var(--white); }
    .plan-btn.ghost:hover { border-color: #555; }
    .plan-btn.green { background: var(--green); color: #080808; }
    .plan-btn.green:hover { opacity: 0.88; }

    /* FINAL CTA */
    .final-cta { border-bottom: none; text-align: center; }
    .final-eyebrow { display: inline-flex; align-items: center; gap: 8px; font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--dim); margin-bottom: 20px; }
    .final-dot { width: 6px; height: 6px; background: var(--red); border-radius: 50%; animation: pulse 1.4s ease-in-out infinite; }
    .final-headline { font-family: 'Bebas Neue', sans-serif; font-size: clamp(52px, 9vw, 96px); letter-spacing: 0.03em; line-height: 1.0; margin-bottom: 16px; }
    .final-sub { font-size: 15px; font-weight: 300; color: #aaa; margin-bottom: 32px; }
    .final-note { font-size: 11px; color: var(--dim); margin-top: 14px; }

    /* FOOTER */
    footer { padding: 32px 0 0; }
    .footer-main { display: flex; justify-content: space-between; align-items: center; padding-bottom: 24px; }
    .footer-tagline { font-size: 12px; color: var(--dim); margin-top: 6px; }
    .footer-links { display: flex; gap: 24px; }
    .footer-links a { font-size: 12px; color: var(--dim); transition: color 0.15s; }
    .footer-links a:hover { color: var(--white); }
    .footer-disclaimer { border-top: 1px solid var(--border); padding: 16px 0; font-size: 11px; color: var(--dim); line-height: 1.6; }

    /* MODAL */
    .modal-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.92); z-index: 500; align-items: center; justify-content: center; padding: 24px; }
    .modal-overlay.open { display: flex; }
    .modal { background: var(--s1); border: 1px solid var(--b2); border-top: 3px solid var(--green); width: 100%; max-width: 400px; padding: 32px 28px; position: relative; }
    .modal-close { position: absolute; top: 14px; right: 14px; background: none; border: none; color: var(--dim); font-size: 20px; line-height: 1; padding: 4px; transition: color 0.15s; }
    .modal-close:hover { color: var(--white); }
    .modal-logo { margin-bottom: 20px; }
    .modal-title { font-family: 'Bebas Neue', sans-serif; font-size: 24px; letter-spacing: 0.06em; color: var(--white); margin-bottom: 20px; }
    .modal-error { font-size: 12px; color: var(--red); min-height: 16px; margin-bottom: 8px; }
    .modal-google { width: 100%; background: var(--s2); border: 1px solid var(--b2); color: var(--white); font-size: 13px; font-weight: 500; padding: 12px; display: flex; align-items: center; justify-content: center; gap: 8px; margin-bottom: 20px; transition: background 0.15s; }
    .modal-google:hover { background: #1a1a1a; }
    .modal-divider { display: flex; align-items: center; gap: 12px; margin-bottom: 20px; }
    .modal-divider span { font-size: 11px; color: var(--dim); }
    .modal-divider::before, .modal-divider::after { content: ''; flex: 1; height: 1px; background: var(--b2); }
    .modal-label { display: block; font-size: 10px; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--dim); margin-bottom: 6px; }
    .modal-input { width: 100%; background: var(--s2); border: 1px solid var(--b2); color: var(--white); font-size: 14px; font-family: 'DM Sans', sans-serif; padding: 11px 12px; outline: none; margin-bottom: 14px; transition: border-color 0.15s; }
    .modal-input:focus { border-color: var(--green); }
    .modal-submit { width: 100%; background: var(--green); color: #080808; font-family: 'Bebas Neue', sans-serif; font-size: 16px; letter-spacing: 0.1em; border: none; padding: 14px; margin-top: 4px; transition: opacity 0.15s; }
    .modal-submit:hover { opacity: 0.88; }
    .modal-switch { font-size: 12px; color: var(--dim); text-align: center; margin-top: 16px; }
    .modal-switch a { color: var(--green); cursor: pointer; }

    /* RESPONSIVE */
    @media(max-width: 720px) {
      .wrap { padding: 0 20px; }
      section { padding: 56px 0; }
      .hero-grid { grid-template-columns: 1fr; gap: 40px; }
      .hero-grid .mockup-card { order: 2; }
      .problem-grid { grid-template-columns: 1fr; gap: 36px; }
      .steps-grid { grid-template-columns: 1fr; }
      .step { border-right: none; border-bottom: 1px solid var(--b2); }
      .step:last-child { border-bottom: none; }
      .features-grid { grid-template-columns: 1fr; }
      .proof-grid { grid-template-columns: 1fr; }
      .who-grid { grid-template-columns: 1fr; }
      .pricing-grid { grid-template-columns: 1fr; max-width: 100%; }
      .footer-main { flex-direction: column; align-items: flex-start; gap: 16px; }
      .nav-signin { display: none; }
    }
    @media(max-width: 480px) {
      .hero-headline { font-size: 38px; }
      .hero-ctas { flex-direction: column; width: 100%; }
      .cta-primary, .cta-secondary { width: 100%; justify-content: center; }
      .final-cta .cta-primary { width: 100%; justify-content: center; }
      .hero { padding: 48px 0 56px; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<div class="wrap">
  <nav class="nav">
    <div class="nav-inner">
      <div class="logo">
        <div class="logo-pk"><span class="w">Parlay</span><span class="g">Killer</span></div>
        <div class="logo-le">Live Edge</div>
      </div>
      <div class="nav-actions">
        <button class="btn-ghost nav-signin" onclick="openModal('login')">Sign In</button>
        <button class="btn-green" onclick="openModal('signup')">
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M1 6l3.5 3.5L11 2" stroke="#080808" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Get Early Access
        </button>
      </div>
    </div>
  </nav>
</div>

<!-- HERO -->
<div class="wrap">
  <section class="hero">
    <div class="hero-grid">
      <div>
        <div class="eyebrow-pill">
          <span class="eyebrow-dot"></span>
          NBA · MLB · Live Now
        </div>
        <h1 class="hero-headline">See what the live total<br><span class="hl-green">should be</span><br>before the market does.</h1>
        <p class="hero-sub">Real-time projections for the next few minutes of the game. Find the edge. Act before it disappears.</p>
        <div class="hero-ctas">
          <a href="#" class="cta-primary" onclick="openModal('signup');return false;">
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M1.5 6.5l3.5 4L11.5 2" stroke="#080808" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            Get Early Access
          </a>
          <a href="#shift" class="cta-secondary">
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><polygon points="3,2 11,6.5 3,11" fill="currentColor"/></svg>
            See It in Action
          </a>
        </div>
        <div class="hero-note">
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><circle cx="6" cy="6" r="5" stroke="#555" stroke-width="1"/><path d="M6 3.5V6l1.5 1.5" stroke="#555" stroke-width="1" stroke-linecap="round"/></svg>
          No account required to preview
        </div>
      </div>
      <div>
        <div class="mockup-card">
          <div class="mc-header">KNICKS @ CELTICS &nbsp;/&nbsp; Q4 · 6:31 · ESPN</div>
          <div class="mc-score-row">
            <div class="mc-team">
              <div class="mc-team-abbr">NYK</div>
              <div class="mc-team-score">90</div>
            </div>
            <div class="mc-center">
              <div class="mc-period">Q4</div>
              <div class="mc-clock">6:31</div>
            </div>
            <div class="mc-team right">
              <div class="mc-team-abbr">BOS</div>
              <div class="mc-team-score">111</div>
            </div>
          </div>
          <div class="mc-data-row"><span class="mc-data-label">Live Total</span><span class="mc-data-val">200.5</span></div>
          <div class="mc-data-row"><span class="mc-data-label">Model Fair</span><span class="mc-data-val green">206.9</span></div>
          <div class="mc-edge-block">
            <div class="mc-edge-num">EDGE: +6.4 OVER</div>
            <div class="mc-edge-sub">Market is 6.4 pts behind reality</div>
          </div>
          <div class="mc-window-row">
            <span class="mc-window-label">Next window</span>
            <span class="mc-window-val">6:31 → 17.8 pts</span>
          </div>
          <div class="mc-badge"><span class="mc-badge-dot"></span>HEATING</div>
        </div>
      </div>
    </div>
  </section>
</div>

<!-- THE PROBLEM -->
<div class="wrap">
  <section>
    <div class="tag">The problem</div>
    <h2 class="section-head">Live betting feels like guessing.</h2>
    <div class="problem-grid">
      <div class="problem-prose">
        <p>You're watching the game. The total moves. You hesitate.</p>
        <br>
        <p>Is the number right? Is this a run — or just noise?</p>
        <br>
        <p>By the time you decide — the line is gone.</p>
      </div>
      <div>
        <div class="line-table">
          <div class="lt-row">
            <span class="lt-label">Total opens</span>
            <span class="lt-val white">214.5</span>
          </div>
          <div class="lt-row">
            <span class="lt-label">Q3 ends, total moves</span>
            <span class="lt-val amber">208.5 ↓</span>
          </div>
          <div class="lt-row">
            <span class="lt-label">You consider the under</span>
            <span class="lt-val amber">205.5 ↓↓</span>
          </div>
          <div class="lt-row">
            <span class="lt-label">You hesitate</span>
            <span class="lt-val red">200.5 ↓↓↓</span>
          </div>
          <div class="lt-row">
            <span class="lt-label">Line closes</span>
            <span class="lt-val muted">Opportunity gone</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>

<!-- THE SHIFT -->
<div class="wrap" id="shift">
  <section>
    <div class="tag">The shift</div>
    <h2 class="section-head">What if you could price<br>the game in real time?</h2>
    <p class="section-sub">Parlay Killer Live Edge doesn't just show odds. It shows what the total should be, how many points are likely in the next few minutes, and exactly where the market is wrong.</p>
    <div class="steps-grid">
      <div class="step">
        <div class="step-num">STEP 01 · TRACK</div>
        <div class="step-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="11" stroke="#555" stroke-width="1.5"/><path d="M14 8v6l3 3" stroke="#555" stroke-width="1.5" stroke-linecap="round"/></svg>
        </div>
        <div class="step-title">We track the game state</div>
        <div class="step-desc">Score, pace, efficiency, fouls, timeouts — everything that actually drives scoring, updated every 30 seconds.</div>
      </div>
      <div class="step">
        <div class="step-num">STEP 02 · PROJECT</div>
        <div class="step-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><polyline points="4,20 10,12 16,16 22,6" stroke="#555" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div class="step-title">We project what happens next</div>
        <div class="step-desc">Not just the final total — the next 2, 5, or 6 minutes. Foul state, blowout risk, garbage time all factored in.</div>
      </div>
      <div class="step">
        <div class="step-num">STEP 03 · EDGE</div>
        <div class="step-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><polygon points="14,4 17,11 24,11 18.5,15.5 20.5,23 14,18.5 7.5,23 9.5,15.5 4,11 11,11" stroke="#555" stroke-width="1.5" fill="none"/></svg>
        </div>
        <div class="step-title">We show where the market is wrong</div>
        <div class="step-desc">When the book lags behind reality, you see it instantly.</div>
        <div class="code-block">
          <div>Market: 200.5</div>
          <div class="cb-green">Model: 206.9</div>
          <div class="cb-green">Edge: +6.4 OVER</div>
        </div>
      </div>
    </div>
  </section>
</div>

<!-- FEATURES -->
<div class="wrap">
  <section>
    <div class="tag">Built for live decisions</div>
    <h2 class="section-head">Everything you need.<br>Nothing you don't.</h2>
    <div class="features-grid">
      <div class="feat-card accent">
        <div class="feat-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><rect x="3" y="3" width="7" height="7" stroke="#555" stroke-width="1.5"/><rect x="14" y="3" width="7" height="7" stroke="#555" stroke-width="1.5"/><rect x="3" y="14" width="7" height="7" stroke="#555" stroke-width="1.5"/><rect x="14" y="14" width="7" height="7" stroke="#555" stroke-width="1.5"/></svg>
        </div>
        <div class="feat-title">The Board</div>
        <div class="feat-desc">All live games ranked by edge. Spot the best opportunity the moment it opens.</div>
        <ul class="feat-list">
          <li>Sorted by edge strength</li>
          <li>Market vs model visible instantly</li>
          <li>Heating · Closing badges</li>
        </ul>
      </div>
      <div class="feat-card">
        <div class="feat-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><polyline points="3,18 8,11 13,14 18,6" stroke="#555" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div class="feat-title">Game View</div>
        <div class="feat-desc">Market vs model visualized. Next-window projections. Signals that explain why.</div>
        <ul class="feat-list">
          <li>Dynamic clock-based windows</li>
          <li>Alt line availability tracker</li>
          <li>Playable-to number</li>
        </ul>
      </div>
      <div class="feat-card">
        <div class="feat-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M6 8a6 6 0 0112 0v4l2 3H4l2-3V8z" stroke="#555" stroke-width="1.5"/><path d="M10 20a2 2 0 004 0" stroke="#555" stroke-width="1.5"/></svg>
        </div>
        <div class="feat-title">Alerts</div>
        <div class="feat-desc">You don't have to watch every second. We do it for you.</div>
        <ul class="feat-list">
          <li>Edge crosses your threshold</li>
          <li>Model flips direction</li>
          <li>Push, in-app, or email</li>
        </ul>
      </div>
    </div>
  </section>
</div>

<!-- PROOF -->
<div class="wrap">
  <section>
    <div class="tag">Proof</div>
    <h2 class="section-head">When it hits, you'll know.</h2>
    <p class="section-sub">It's not about being perfect. It's about seeing the edge before everyone else does.</p>
    <div class="proof-grid">
      <div class="proof-card">
        <div class="proof-title">Window projection vs actual</div>
        <div class="proof-row"><span class="proof-label">Projected (next 5:00)</span><span class="proof-val">13.2 pts</span></div>
        <div class="proof-row"><span class="proof-label">Actual scored</span><span class="proof-val green">14 pts ✓</span></div>
        <div class="proof-row"><span class="proof-label">Variance</span><span class="proof-val green">+0.8</span></div>
        <div class="proof-quote">"Third time in a row the 5-minute window came within 1.5 pts."</div>
      </div>
      <div class="proof-card">
        <div class="proof-title">Edge capture</div>
        <div class="proof-row"><span class="proof-label">Model edge at entry</span><span class="proof-val green">+5.8 OVER</span></div>
        <div class="proof-row"><span class="proof-label">Closing edge</span><span class="proof-val">+2.1</span></div>
        <div class="proof-row"><span class="proof-label">Result</span><span class="proof-val green">Over hit ✓</span></div>
        <div class="proof-quote">"Saw it at +5.8. By the time I placed it the edge was already shrinking. Got in just in time."</div>
      </div>
    </div>
  </section>
</div>

<!-- WHO -->
<div class="wrap">
  <section>
    <div class="tag">Who it's for</div>
    <h2 class="section-head">Not picks. Not trends.<br>Not guesses.</h2>
    <p class="section-sub">This isn't another betting app. We don't tell you what might happen. We show you what the number should be — right now.</p>
    <div class="who-grid">
      <div class="who-card">
        <div class="who-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M14 4c5.5 0 10 4.5 10 10s-4.5 10-10 10S4 19.5 4 14" stroke="#555" stroke-width="1.5" stroke-linecap="round"/><path d="M4 8l0 6 6 0" stroke="#555" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div class="who-title">Live bettors who act fast</div>
        <div class="who-desc">You need a read in under 10 seconds. The board gives you one.</div>
      </div>
      <div class="who-card">
        <div class="who-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#555" stroke-width="1.5"/><path d="M14 9v5l3 3" stroke="#555" stroke-width="1.5" stroke-linecap="round"/></svg>
        </div>
        <div class="who-title">People tired of guessing totals</div>
        <div class="who-desc">The model tells you when the book is wrong. You decide what to do with it.</div>
      </div>
      <div class="who-card">
        <div class="who-icon">
          <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="10" r="4" stroke="#555" stroke-width="1.5"/><path d="M6 24c0-4.4 3.6-8 8-8s8 3.6 8 8" stroke="#555" stroke-width="1.5" stroke-linecap="round"/></svg>
        </div>
        <div class="who-title">Anyone who watches games differently</div>
        <div class="who-desc">Once you see the model vs market gap, you can't unsee it.</div>
      </div>
    </div>
  </section>
</div>

<!-- PRICING -->
<div class="wrap">
  <section>
    <div class="tag">Pricing</div>
    <h2 class="section-head">Simple. No surprises.</h2>
    <div class="pricing-grid">
      <div class="plan">
        <div class="plan-name">Free</div>
        <div class="plan-price">$0</div>
        <div class="plan-note">Always free</div>
        <ul class="plan-list">
          <li>Limited board access</li>
          <li>Delayed updates</li>
          <li>3 edge analyses</li>
        </ul>
        <button class="plan-btn ghost" onclick="openModal('signup')">Get Started</button>
      </div>
      <div class="plan featured">
        <div class="plan-name">Live Edge</div>
        <div class="plan-price">TBD</div>
        <div class="plan-note">Revealed at launch</div>
        <ul class="plan-list">
          <li>Real-time projections</li>
          <li>Full board access</li>
          <li>Unlimited analyses</li>
          <li>Push alerts</li>
          <li>Alt line tracker</li>
        </ul>
        <button class="plan-btn green" onclick="openModal('signup')">Join Waitlist</button>
      </div>
    </div>
  </section>
</div>

<!-- FINAL CTA -->
<div class="wrap">
  <section class="final-cta">
    <div class="final-eyebrow"><span class="final-dot"></span>The edge doesn't wait</div>
    <h2 class="final-headline">The edge doesn't last.</h2>
    <p class="final-sub">Get access before the market catches up.</p>
    <a href="#" class="cta-primary" onclick="openModal('signup');return false;" style="display:inline-flex;">
      <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M1.5 6.5l3.5 4L11.5 2" stroke="#080808" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
      Join the Waitlist
    </a>
    <div class="final-note">No credit card · No commitment · Early access pricing locked in</div>
  </section>
</div>

<!-- FOOTER -->
<div class="wrap">
  <footer>
    <div class="footer-main">
      <div>
        <div class="logo">
          <div class="logo-pk"><span class="w">Parlay</span><span class="g">Killer</span></div>
          <div class="logo-le">Live Edge</div>
        </div>
        <div class="footer-tagline">Price the next few minutes of the game.</div>
      </div>
      <div class="footer-links">
        <a href="/privacy">Privacy</a>
        <a href="/terms">Terms</a>
        <a href="mailto:hello@parlaykillerapp.com">Contact</a>
      </div>
    </div>
    <div class="footer-disclaimer">
      Parlay Killer Live Edge is a math tool only — not betting advice. Must be 21+ in applicable jurisdictions. Please gamble responsibly. <a href="https://www.ncpgambling.org" style="color:var(--dim);text-decoration:underline;">ncpgambling.org</a>
    </div>
  </footer>
</div>

<!-- LOGIN MODAL -->
<div class="modal-overlay" id="modal-login" onclick="if(event.target===this)closeModal('login')">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('login')">&times;</button>
    <div class="modal-logo">
      <div class="logo"><div class="logo-pk"><span class="w">Parlay</span><span class="g">Killer</span></div><div class="logo-le">Live Edge</div></div>
    </div>
    <div class="modal-title">Sign In</div>
    <div class="modal-error" id="login-error"></div>
    <button class="modal-google" onclick="window.location.href='/api/auth/google'">
      <svg width="16" height="16" viewBox="0 0 16 16"><path d="M15.68 8.18c0-.57-.05-1.11-.14-1.64H8v3.1h4.3a3.67 3.67 0 01-1.6 2.41v2h2.6c1.52-1.4 2.4-3.46 2.4-5.87z" fill="#4285F4"/><path d="M8 16c2.16 0 3.97-.72 5.3-1.94l-2.6-2a4.78 4.78 0 01-2.7.75 4.74 4.74 0 01-4.46-3.28H.88v2.06A8 8 0 008 16z" fill="#34A853"/><path d="M3.54 9.53A4.8 4.8 0 013.3 8c0-.53.1-1.04.25-1.53V4.41H.88A8 8 0 000 8c0 1.29.31 2.51.88 3.59l2.66-2.06z" fill="#FBBC05"/><path d="M8 3.18c1.22 0 2.31.42 3.17 1.24l2.38-2.38A8 8 0 00.88 4.41l2.66 2.06A4.74 4.74 0 018 3.18z" fill="#EA4335"/></svg>
      Continue with Google
    </button>
    <div class="modal-divider"><span>or</span></div>
    <div>
      <label class="modal-label">Email</label>
      <input class="modal-input" type="email" id="login-email" placeholder="you@example.com" autocomplete="email">
    </div>
    <div>
      <label class="modal-label">Password</label>
      <input class="modal-input" type="password" id="login-password" placeholder="••••••••" autocomplete="current-password">
    </div>
    <button class="modal-submit" onclick="submitLogin()">Sign In</button>
    <div class="modal-switch">No account? <a onclick="switchModal('login','signup')">Sign up free</a></div>
  </div>
</div>

<!-- SIGNUP MODAL -->
<div class="modal-overlay" id="modal-signup" onclick="if(event.target===this)closeModal('signup')">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('signup')">&times;</button>
    <div class="modal-logo">
      <div class="logo"><div class="logo-pk"><span class="w">Parlay</span><span class="g">Killer</span></div><div class="logo-le">Live Edge</div></div>
    </div>
    <div class="modal-title">Create Account</div>
    <div class="modal-error" id="signup-error"></div>
    <button class="modal-google" onclick="window.location.href='/api/auth/google'">
      <svg width="16" height="16" viewBox="0 0 16 16"><path d="M15.68 8.18c0-.57-.05-1.11-.14-1.64H8v3.1h4.3a3.67 3.67 0 01-1.6 2.41v2h2.6c1.52-1.4 2.4-3.46 2.4-5.87z" fill="#4285F4"/><path d="M8 16c2.16 0 3.97-.72 5.3-1.94l-2.6-2a4.78 4.78 0 01-2.7.75 4.74 4.74 0 01-4.46-3.28H.88v2.06A8 8 0 008 16z" fill="#34A853"/><path d="M3.54 9.53A4.8 4.8 0 013.3 8c0-.53.1-1.04.25-1.53V4.41H.88A8 8 0 000 8c0 1.29.31 2.51.88 3.59l2.66-2.06z" fill="#FBBC05"/><path d="M8 3.18c1.22 0 2.31.42 3.17 1.24l2.38-2.38A8 8 0 00.88 4.41l2.66 2.06A4.74 4.74 0 018 3.18z" fill="#EA4335"/></svg>
      Continue with Google
    </button>
    <div class="modal-divider"><span>or</span></div>
    <div>
      <label class="modal-label">Name</label>
      <input class="modal-input" type="text" id="signup-name" placeholder="Your name" autocomplete="name">
    </div>
    <div>
      <label class="modal-label">Email</label>
      <input class="modal-input" type="email" id="signup-email" placeholder="you@example.com" autocomplete="email">
    </div>
    <div>
      <label class="modal-label">Password (min 8 chars)</label>
      <input class="modal-input" type="password" id="signup-password" placeholder="••••••••" autocomplete="new-password">
    </div>
    <button class="modal-submit" onclick="submitSignup()">Create Account</button>
    <div class="modal-switch">Already have an account? <a onclick="switchModal('signup','login')">Sign in</a></div>
  </div>
</div>

<script>
function openModal(name) {
  document.getElementById('modal-' + name).classList.add('open');
  if (name === 'login') setTimeout(() => document.getElementById('login-email').focus(), 60);
  if (name === 'signup') setTimeout(() => document.getElementById('signup-email').focus(), 60);
}
function closeModal(name) {
  document.getElementById('modal-' + name).classList.remove('open');
}
function switchModal(from, to) {
  closeModal(from);
  openModal(to);
}
async function submitLogin() {
  const email = document.getElementById('login-email').value;
  const password = document.getElementById('login-password').value;
  const err = document.getElementById('login-error');
  err.textContent = '';
  if (!email || !password) { err.textContent = 'Email and password required'; return; }
  try {
    const r = await fetch('/api/auth/login', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      credentials: 'include', body: JSON.stringify({ email, password }),
    });
    const data = await r.json();
    if (!r.ok) { err.textContent = data.error || 'Login failed'; return; }
    window.location.href = '/';
  } catch(e) { err.textContent = 'Something went wrong. Try again.'; }
}
async function submitSignup() {
  const name = document.getElementById('signup-name').value;
  const email = document.getElementById('signup-email').value;
  const password = document.getElementById('signup-password').value;
  const err = document.getElementById('signup-error');
  err.textContent = '';
  if (!email || !password) { err.textContent = 'Email and password required'; return; }
  if (password.length < 8) { err.textContent = 'Password must be at least 8 characters'; return; }
  try {
    const r = await fetch('/api/auth/register', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      credentials: 'include', body: JSON.stringify({ name, email, password }),
    });
    const data = await r.json();
    if (!r.ok) { err.textContent = data.error || 'Registration failed'; return; }
    window.location.href = '/';
  } catch(e) { err.textContent = 'Something went wrong. Try again.'; }
}
// Auth check — redirect if already logged in
fetch('/api/auth/me', { credentials: 'include' })
  .then(r => { if (r.ok) window.location.href = '/'; })
  .catch(() => {});
</script>
</body>
</html>
